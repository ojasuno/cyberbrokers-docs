import Bleed from 'nextra-theme-docs/bleed'
import React, {useRef, useEffect} from "react";
import {Runtime, Inspector} from "@observablehq/runtime";
import notebook from "@mbostock/polar-clock";

# ERANOVUM | 3R4N0VUM

<Bleed>![Source](../public/_assets/_cyberbroker-banner/cb-00000.png)</Bleed>


function PolarClock() {
  const chartRef = useRef();

  useEffect(() => {
    const runtime = new Runtime();
    runtime.module(notebook, name => {
      if (name === "chart") return new Inspector(chartRef.current);
    });
    return () => runtime.dispose();
  }, []);

  return (
    <>
      <div ref={chartRef} />
      <p>Credit: <a href="https://observablehq.com/@mbostock/polar-clock">Polar Clock by Mike Bostock</a></p>
    </>
  );
}

export default PolarClock;